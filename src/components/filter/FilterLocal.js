import { StyleSheet, Text, View } from 'react-native';
import React, { memo } from 'react';

const FilterLocal = () => {
  return (
    <View>
      <Text>FilterLocal</Text>
    </View>
  );
};

export default memo(FilterLocal);

const styles = StyleSheet.create({});
