import { StyleSheet, Text, View } from 'react-native';
import React, { memo } from 'react';

const FilterOnline = () => {
  return (
    <View>
      <Text>FilterOnline</Text>
    </View>
  );
};

export default memo(FilterOnline);

const styles = StyleSheet.create({});
