import { StyleSheet, Text, View } from 'react-native';
import React, { memo } from 'react';

const CreateGatheringLocal = () => {
  return (
    <View>
      <Text>CreateGatheringLocal</Text>
    </View>
  );
};

export default memo(CreateGatheringLocal);

const styles = StyleSheet.create({});
